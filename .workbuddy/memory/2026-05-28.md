# 2026-05-28

## 修复 Qdrant 本地模式双客户端文件锁冲突

**问题**：`rag/ingest.py` 执行时报 `RuntimeError: Storage folder ./data/qdrant_db is already accessed by another instance of Qdrant client`

**根因**：`ingest.py` 中第62行创建了第一个 `QdrantClient` 做 Collection 删除/创建操作，第82行调用 `get_or_create_vector_store()` 内部又创建了第二个 `QdrantClient`，两个实例指向同一 `./data/qdrant_db` 路径，Qdrant 本地模式的文件锁只允许一个客户端访问。

**修复**：改为直接用已有 client 构造 QdrantVectorStore，不再重复创建（真正的根因是逻辑冗余）。

## 修复向量维度不匹配

`EMBEDDING_DIM` 从 1536 改为 1024，因为 `text-embedding-v3` 实际输出 1024 维。

## 日志系统重构

- `config/logger.py`：改为单例模式 `get_logger()`，避免重复添加 handler 导致日志刷屏
- `graph/workflow.py`：所有 5 个节点加完整 try/except + 日志，supervisor 路由加追踪，Redis 连接失败降级为 MemorySaver

## 生命周期重构 + Redis 持久化架构

- `graph/workflow.py`：模块级 `builder.compile()` 改为工厂函数 `build_agent_app(checkpointer=None)`，支持外部传入 Redis 或 MemorySaver
- `main.py`：FastAPI `@asynccontextmanager lifespan` 启动时按 `REDIS_URL` 环境变量选择存储后端，新增 `/history/{session_id}` 和 `/health` 接口

## 补充 requirements.txt + 生成 README.md

- 补全了缺失的依赖：langgraph-checkpoint-redis、dashscope、docling、redis、portalocker 等
- README 包含架构图、技术栈、快速开始、API 文档、联手协作说明
